
  <p>
    <strong>By user:</strong>
    <%= comment.user_id %>
  </p>

  <p>
    <strong>Comment:</strong>
    <%= comment.body %>
  </p>

  <% if current_user.id == comment.user_id %>
    <p>
      <%= link_to 'Destroy Comment', [comment.property, comment],
                   method: :delete,
                   data: { confirm: 'Are you sure?' } %>
    </p>
    <% end %>
